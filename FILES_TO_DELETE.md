# Файли, які можна видалити (не використовуються, проект 99.9% готовий)

## ✅ Безпечно видалити — ніде не імпортуються

| Файл | Чому можна видалити |
|------|----------------------|
| **src/pages/index.tsx** | Старий варіант головної (Main). У роутері використовується **HomePage.tsx**; цей файл не імпортується в App.tsx. |
| **src/pages/DashboardMenuPage.tsx** | Меню кабінету вже вшите в **DashboardHeader.tsx**. Цей окремий компонент ніде не імпортується. |
| **src/pages/HumburgerMenuPage.tsx** | Бургер-меню вже вшите в **Header.tsx**. Цей окремий компонент ніде не імпортується. |
| **src/pages/RegistestrationPage.tsx** | Сторінка реєстрації не підключена до роутів (немає `/register`). Якщо не плануєш додавати реєстрацію — можна видалити. |
| **src/components/SimpleBarWrapper.tsx** | Ніде не імпортується. SimpleBar підключено в index.html і може використовуватись напряму; цей wrapper не використовується. |

---

## ⚠️ Опціонально (якщо не потрібні)

| Файл / дія | Примітка |
|------------|----------|
| **src/utils/confetti.ts** | Функція конфетті ніде не викликається. Можна видалити файл. Якщо видалиш — у **index.html** прибери рядок `<script src="/lib/confetti.browser.min.js"></script>`, а в **src/global.d.ts** — рядок `confetti: (options?: object) => void;` з `Window`. |
| **public/lib/confetti.browser.min.js** | Видалити разом із confetti.ts, якщо не плануєш конфетті. |
| **public/lib/anime.min.js** | Бібліотека anime.js не використовується в коді. Можна прибрати з **index.html** і видалити файл. У **global.d.ts** прибери `anime: ...` з `Window`. |

---

## 📄 Документація (на твій розсуд)

| Файл | Рекомендація |
|------|--------------|
| **HOMEPAGE_COMPARISON.md** | Порівняння HomePage vs index.tsx. Після видалення index.tsx можна видалити й цей файл. |
| **CODIA_PAGES_MAPPING.md** | Таблиця відповідності Codia → сторінки. Корисно залишити для довідки; можна видалити, якщо не потрібна. |

---

## ❌ Не видаляти

- **src/pages/index.css** — імпортується всіма сторінками.
- **src/img/1st_block_backgound.webp** — використовується в index.css (мапа, hero).
- Усі сторінки, що в **App.tsx** (HomePage, LoginPage, Dashboard*, Handbook*, Contact*, Referral*, Inzhur*, тощо).
- **Layout, Header, DashboardHeader, Footer, AuthContext** — використовуються.
- **public/lib/gsap.min.js**, **ScrollTrigger.min.js**, **simplebar.min.js** / **simplebar.min.css** — підключені в index.html і використовуються (gsap+ScrollTrigger у HomePage, simplebar в index.html).
